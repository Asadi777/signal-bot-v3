# Code Quick Reference (CEC 2024)

> **Source:** `code quick refrence.pdf` (Google Drive fileId `1-4eS2NfyRchUIyRZQHgS3pnDe4T4prxM`), ~43 MB, 206 pages.
> **Intended use:** Quick-lookup study aid for the BC Construction Electrician / CEC 2024 certification exam.

## Ingestion Status — IMPORTANT

**This PDF has NO text layer. It is a scanned / image-only PDF.**

- **Text layer present:** No.
- **Fraction of usable text extracted:** 0% (0 of 206 pages).
- **What the reader returned:** The Google Drive content reader processed all 206 pages, but every single page came back as an empty image placeholder (rendered as `[**Page N**]()` with no associated text). The document begins with the title "Untitled" and contains nothing but 206 blank image cells — no OCR text, no tables, no values.
- **Truncation:** None. The full structure of all 206 pages was returned and inspected; the absence of text is complete, not a side effect of truncation.

### Why no content could be captured

A 43 MB / 206-page PDF that renders as pure images is, as anticipated, a **scanned document** (photographs or image scans of printed pages). Such files store each page as a raster image with no embedded selectable/searchable text. The available Drive reader does not perform OCR on the image content, so no ampacity values, conduit/box-fill percentages, demand factors, motor multipliers, grounding/bonding sizes, voltage-drop figures, or trade-size conversions could be extracted.

### To make this material usable

One of the following is required before exam content can be ingested:

1. **Run OCR** on the source PDF (e.g., Adobe Acrobat "Recognize Text", `ocrmypdf`, Google Drive "Open with Google Docs" which OCRs images, or Tesseract) to produce a text layer, then re-ingest.
2. **Obtain a text-based (born-digital) version** of the same quick-reference document, if one exists.
3. **Provide page images directly** to a vision-capable model for transcription of the specific tables/values needed.

---

## Quick-Reference Content

_None captured._ The source contained no extractable text. This section is intentionally empty and should be populated only after the source is OCR'd or replaced with a text-bearing version. The originally requested topics to look for once readable:

- Ampacity tables (CEC Table 1–4, correction/derating factors)
- Conduit and box fill percentages (Tables 6, 8, 9, 10; Section 12)
- Demand factors (Section 8)
- Motor multipliers and full-load currents (Section 28, Table 44 / 45)
- Grounding and bonding conductor sizes (Section 10, Table 16)
- Voltage-drop calculations and allowances (Section 8-102)
- Trade-size / metric conversions

> Do not treat anything in this section as study content yet — there is none.
